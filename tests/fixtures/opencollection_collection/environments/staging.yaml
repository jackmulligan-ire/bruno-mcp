name: staging
variables:
  - name: baseUrl
    value: http://localhost:3000
    enabled: true
    secret: false
    type: text
  - name: authToken
    value: "{{process.env.SECRET_TOKEN}}"
    enabled: true
    secret: true
    type: text
