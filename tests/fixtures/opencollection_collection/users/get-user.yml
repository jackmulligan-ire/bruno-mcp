info:
  name: Get User
  type: http
  seq: 1

http:
  method: GET
  url: https://api.example.com/users/{{userId}}
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  params:
    - name: limit
      value: "10"
      type: query

settings:
  encodeUrl: true
